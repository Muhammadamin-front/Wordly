# Context for apps/web/Dockerfile (built from the repo root).
node_modules
apps/web/node_modules
apps/web/.next
apps/api
docs
.git
.github
.agents
skills-lock.json
*.md
.env*

# Duplicate trees (see .gitignore): never part of the build context — they
# added ~500MB to every image build.
* 2
* 2/
node_modules 2
apps 2
.git 2
