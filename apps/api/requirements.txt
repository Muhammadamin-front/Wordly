# Direct dependencies only, with the ranges we support. Nothing installs from
# this file directly: the image and CI install requirements.lock.txt, which is
# the resolved, pinned version of exactly this list. Change a range here, then
# regenerate the lock (see its header).
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20
asyncpg>=0.29
alembic>=1.13
pydantic>=2.8
pydantic-settings>=2.4
email-validator>=2.1
bcrypt>=4.1
PyJWT>=2.8
cryptography>=43.0
httpx>=0.27
redis>=5.0
python-multipart>=0.0.9
tzdata>=2024.1
sentry-sdk[fastapi]>=2.18
fsrs==3.1.0
