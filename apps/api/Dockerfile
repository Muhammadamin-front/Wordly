FROM python:3.12-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /srv/api

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8000

# Run migrations, import bundled learning content, then serve.
CMD ["sh", "-c", "alembic upgrade head && python -m scripts.import_expressions && uvicorn app.main:app --host 0.0.0.0 --port 8000"]
